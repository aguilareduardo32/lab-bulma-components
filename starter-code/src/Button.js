import React from "react";
const Buttons = props => {
    return (
        <button className={props.class}>Button 1</button>
        <button className={props.class}>Button 2</button>
     )
}

export default Buttons